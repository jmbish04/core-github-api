Here is the transcription of the provided audio recording:

This is my core GitHub API worker; it is intended to be my go-to resource for developing code for any type of project, but specifically for building Cloudflare Workers. Sometimes it might be used for Python scripts, App scripts, or other project types—maybe Vercel—so it is supposed to be flexible. Primarily, it relates to CI/CD and maintaining consistency in how my workers are operating with best practices, as well as training and controlling the guardrails of AI coding agents.

To help with that mission, I have tried to build in some tools and UX elements, such as:

* **Cloudflare Docs MCP**: A dedicated agent that is an expert in Cloudflare documentation to provide advice.
* **Jules**: Used to do most of the heavy lifting, though it needs heavy orchestration because it is not always consistent and gets stuck on basic things.
* **Stitch**: Handles UX mockups.
* **GitHub Integration**: Specifically, making GitHub easier to use. I noticed that when using "Gemini Review" (a coding review bot) on pull requests (PRs), agents like Jules or Copilot sometimes claim they don't have access to the comments. I added a tool to this worker where you paste the PR URL, and it extracts the Gemini code comments to the clipboard so I can paste them directly to the agent.

I am trying to leverage many things to make me a better coder and to improve my team of agents. One thing I have not been able to achieve yet—and I would like you to conduct some research on how to achieve this—is targeted research against GitHub and other sources like Discord where Cloudflare has a following.

I think there are primarily three different use cases I want to support with this:

1. **On-Demand Research**: This is for when I am trying to solve a specific problem or achieve a goal. Rather than designing from scratch, I want to see what others have done and shared on GitHub to inform that decision (for example, building a smart home IoT sensor that connects to various APIs). I want an agent or workflow that can dynamically conduct research starting with search terms, reviewing initial results, and refining the search over a couple of rounds based on what it finds. It should be able to read README sections and even code files to understand what the code is doing.
2. **Pre-Planning Research**: This would be done at the start of a project to influence how I design and structure the build. I want to find lessons learned, frameworks, or implementations from others. For example, finding workarounds for Cloudflare Worker limitations at the edge using Durable Objects or seeing how others set up their Cloudflare Agents SDK. This helps before we begin drafting PRDs and task planning.
3. **General Awareness**: A light-scale, recurring research task (perhaps weekly) to keep me aware of cool things people are doing with Cloudflare Workers, App scripts, or Vercel. It would look for new products, features, or significant updates (like a tool moving from beta to full production) and send me an email summary with links to the full findings on my worker's front end. It should be aware of what it has already told me so it isn’t repetitive.

In terms of technical implementation, the GitHub API can be limited when trying to pull the actual content of files. I am considering using the **Sandbox SDK for Cloudflare**, which provides a containerized environment where an agent can have basic controls to clone a GitHub repository and poke around the file system rather than relying on Octokit.

The workflow might involve creating a dedicated GitHub repository for research jobs where we clone repositories as subfolders. Then, we could issue **Jules** (running Gemini 3.1) to do the research because of its million-token context window and its ability to run on its own VM. My Cloudflare worker would orchestrate the research workflow; if Jules has a question or gets stuck, the orchestrator responds in real time. Once finished, the agent running on my core GitHub API worker synthesizes the findings and saves them as a record in **D1** so the user doesn't have to wait for the process to finish, which could take an hour. We should log every source to ensure we aren't duplicating effort within a session or across the weekly awareness use case. This creates a rich data set in D1 that the agent can use to learn over time what constitutes "good" content versus content that didn't fit the goal.

I'm at the beginning stages of planning this. Use your Cloudflare Docs MCP tool and do some research now with this prompt. Let's "dogfood" this: go out to GitHub and Discord to see how others are achieving this type of automated research and extraction. Look at GitHub trending lists, highly starred repositories for keywords like "Cloudflare Worker" or "Shadcn on Cloudflare," and see what GitHub Actions people might be using to pull this data into their own repositories. Explore the technical details of how I should design this system to work in the worker.
